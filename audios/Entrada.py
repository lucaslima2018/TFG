class Entrada:

    def __init__(self, numero_canais, numero_bytes, taxa_amostragem, numero_frames, frames, deltaX, tempo):
        self.numero_canais = numero_canais
        self.numero_bytes = numero_bytes
        self.taxa_amostragem = taxa_amostragem
        self.numero_frames = numero_frames
        self.frames = frames
        self.deltaX = deltaX
        self.tempo = tempo

